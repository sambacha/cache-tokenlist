{
  "name": "tokenlist-cache",
  "version": "0.1.10",
  "description": "Caches tokenlists. Updates when etag changes and is uses cache regardless if endpoint unreachable. Uses write-file-atomic for safe updates",
  "keywords": [
    "etag",
    "web3",
    "cache",
    "tokenlist"
  ],
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "get-remote": "^0.6.1",
    "mkpath": "^1.0.0",
    "rimraf": "^3.0.2",
    "write-file-atomic": "^4.0.1"
  },
  "devDependencies": {
    "fs-access-sync-compat": "^1.0.1",
    "jest": "^28.0.1",
    "mocha": "^9.2.2",
    "prettier": "^2.4.1"
  },
  "engines": {
    "node": ">=0.8"
  }
}
